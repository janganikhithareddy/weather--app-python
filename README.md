# Weather App

Simple Python program to fetch current weather for any city using OpenWeatherMap API.  

**Features:**
- Works in Python IDLE
- Handles spaces in city names
- Shows temperature & weather description

**How to run:**
1. Replace `YOUR_API_KEY` with your OpenWeatherMap API key
2. Run `python weather_app.py` in Python IDLE
